__pycache__/
*.pyc
.DS_Store

# Zdrojový dokument zůstává jen lokálně, do repozitáře se nepublikuje.
# Kvůli tomu nelze z čistého klonu spustit tools/docx2html.py ani
# tools/check_fidelity.py — oba potřebují .docx v adresáři source/.
*.docx
source/

# Mezivýsledek konvertoru etopedie — nic ho nepotřebuje, kapitoly se u etopedie
# poznají ze stylů Heading1. (Naopak tools/_meta_psychologie.json ponechán
# ve verzování: obsahuje mapu „stránka → rozsah bloků“, kterou používá
# check_fidelity.py --map, protože psychologický dokument nadpisové styly nemá.)
tools/_meta.json
